<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.monitoreo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Monitoreo"
        android:usesCleartextTraffic="true">
        <activity android:name=".ImagenAlerta"></activity>
        <activity android:name=".HijoInformacion" />
        <activity android:name=".ModificarDatos" />
        <activity
            android:name=".Menu"
            android:label="Activity Title"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AgregarHijo" />
        <activity android:name=".DescargarImagen" />
        <activity android:name=".RecuperarContra" />
        <activity android:name=".Registrar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>